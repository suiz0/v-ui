{
  "name": "bootstrap-ui",
  "version": "1.0.0",
  "description": "a component library for building web app",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "concurrently \"tsc -w\"  \"lite-server\"",
    "build": "tsc"
  },
  "author": "cesar iduarte",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^5.1.0",
    "lite-server": "^2.5.4",
    "typescript": "^3.8.3"
  }
}
