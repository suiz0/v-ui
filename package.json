{
  "name": "bootstrap-ui",
  "version": "1.0.0",
  "description": "a component library for building web app",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"tsc -w\"  \"lite-server\"",
    "build:debug": "tsc && rollup -c rollup.config.dev.js",
    "build": "tsc && rollup -c rollup.config.prod.js",
    "build:clear": "rm -fr dist"
  },
  "author": "cesar iduarte",
  "license": "ISC",
  "devDependencies": {
    "@rollup/plugin-multi-entry": "^3.0.0",
    "concurrently": "^5.1.0",
    "lite-server": "^2.5.4",
    "rollup": "^2.7.2",
    "rollup-plugin-scss": "^2.4.0",
    "rollup-plugin-terser": "^5.3.0",
    "typescript": "^3.8.3"
  }
}
